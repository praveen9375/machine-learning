#The first function to complete is get_min_max, which returns the overall minimum and maximum element in data.
def get_min_max(data):
  overall_min = data.min()
  overall_max = data.max()
  return overall_min, overall_max
